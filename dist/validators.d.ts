import { CaseType } from './types';
/**
 * Memeriksa apakah sebuah string nama valid berdasarkan aturan case yang diberikan.
 */
export declare function validateCase(name: string, rule: CaseType): boolean;
